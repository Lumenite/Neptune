apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: production
  name: {{ .name }}-{{ .version }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: {{ .storageClass }}
